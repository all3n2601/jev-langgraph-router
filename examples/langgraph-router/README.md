# LangGraph router example

Planned four-route example: search, calculate, answer directly, or request human review. It will
support deterministic offline fixtures and an explicitly enabled live Jev mode.
