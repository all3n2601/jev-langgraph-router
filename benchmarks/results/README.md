# Result registry

Curated benchmark summaries live here. Raw run directories are ignored by default and must be
attached immutably to a release with recorded checksums before any public performance claim.
